import { default as PreloadLink, PreloadLinkInit, PRELOAD_FAIL } from './PreloadLink';

PreloadLink.PRELOAD_FAIL = PRELOAD_FAIL;
PreloadLink.PreloadLinkInit = PreloadLinkInit;

export default PreloadLink;
